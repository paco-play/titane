# __PROJECT_NAME__

Titane game. In development the editor is at `/titane`. A production build strips it.

```bash
npm run dev
```

- Game: http://localhost:3000
- Editor: http://localhost:3000/titane

Write components in `src/components/`, list them in `titane.config.ts`, attach them with Add Component.
